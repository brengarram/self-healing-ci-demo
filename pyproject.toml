[project]
name = "self-healing-ci-demo"
version = "0.1.0"
description = "Repo for CI + Codex autofix + grading demo"
requires-python = ">=3.11"

dependencies = [
    "pytest>=8.0.0",
]

[tool.pytest.ini_options]
pythonpath = ["."]